initSidebarItems({"struct":[["BenchmarkConfig","The numbers configured here could always be more than the the maximum limits of staking pallet to ensure election snapshot will not run out of memory. For now, we set them to smaller values since the staking is bounded and the weight pipeline takes hours for this single pallet."],["UseNominatorsAndUpdateBagsList","Implementation of `frame_election_provider_support::SortedListProvider` that updates the bags-list but uses [`pallet_staking::Nominators`] for `iter`. This is meant to be a transitionary implementation for runtimes to “test” out the bags-list by keeping it up to date, but not yet using it for snapshot generation. In contrast, a  “complete” implementation would use bags-list for `iter`."]],"type":[["GenesisElectionOf","The election provider of the genesis"],["OnOnChainAccuracy","The accuracy type used for genesis election provider;"]]});